import React from 'react';
import { Layout } from '../components';
import { SignIn as SignInComponent } from '../features/sign-in';

export const SignIn = () => (
  <Layout>
    <SignInComponent />
  </Layout>
);
